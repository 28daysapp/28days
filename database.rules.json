{
  "rules": {
    ".read": "true",
    ".write": "true",
    "users": {
      ".write": "auth != null",
      "username": {
        ".validate": "!root.child('usernames').child(newData.val()).exists()"
      }
    },
    "usernames": {
      ".write": "auth != null"
    }
  }
}
